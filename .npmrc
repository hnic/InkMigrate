# peer 解析策略：不自动注入未声明的 peer，未满足/冲突的 peer 范围直接失败
# （当前无 peer 声明；testkit 曾声明 vitest peer，但在 injectWorkspacePackages
#  下触发 pnpm 注入快照、CI 缺 dist 解析失败，已移除，见对应 commit）
auto-install-peers=false
strict-peer-dependencies=true
